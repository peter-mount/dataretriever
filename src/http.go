// Handles http polling mode

package main

//import (
//  "log"
//)

type Http struct {
  config      string       // Pointer to global config
}

var httpConfig Http

func httpInit( ) {
  //httpConfig.config = config
}
